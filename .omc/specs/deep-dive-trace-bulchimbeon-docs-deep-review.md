# Deep Dive Trace: bulchimbeon-docs-deep-review

조사 대상: `C:\Users\kanct\t1\bulchimbeon\` (docs 9 + prompts 10 + CLAUDE.md + README.md = 21개)
레인: L1 내부 정합성(critic/opus) · L2 실현가능성(opus+실측) · L3 전제 감사(opus+실증)

---

## Observed Result

불침번 백엔드 프로젝트 시작 문서 세트를 작성했고, 이것으로 Claude Code가 M0~M9를 순차 실행해 1주 안에 해커톤 데모를 만들 수 있는지 검증이 필요했다.

관찰: 문서는 표면 품질이 매우 높다(우선순위 규칙 명시, D1~D10 엣지 케이스 결정 노트, TODO 인계 4/4 폐쇄, 임계값 설정화 8/8 스키마 반영). 그러나 3개 레인이 독립적으로 조사한 결과 **총 60여 건의 결함**이 나왔고, 그중 **10건이 Blocker**다. 결정적으로 **세 레인이 서로 다른 경로로 조사했는데 같은 단일 근본 원인에 도달했다.**

---

## Ranked Hypotheses

| Rank | Hypothesis | Confidence | Evidence Strength | Why it leads |
| --- | --- | --- | --- | --- |
| 1 | **L3 전제 결함** — 문서가 검증하지 않은 외부 사실(임베딩 유사도 스케일) 위에 핵심 차별점 전체가 서 있다 | **High** | **Strong** (외부 1차 실증 + 형식 증명) | L1·L2의 최상위 발견이 전부 이 하나로 환원된다. 유일하게 문서 수정만으로 해소 불가능한 종류의 결함 |
| 2 | **L2 실현가능성** — 문서대로 구현하면 조용히 틀린 답을 내거나 일정이 성립하지 않는다 | **High** | **Strong** (공식 문서 URL 직접 확인 6건) | pgvector 거리/유사도 반전, GPT-5 파라미터, 세션 수명, 다중 워커 — 전부 1차 소스로 확정. 일정 96~153h vs 7일 |
| 3 | **L1 내부 정합성** — 상태 모델의 폐쇄성 실패로 구현 중 마이그레이션 유발 | **High** | **Strong** (문서 직접 인용, 자기모순 4건) | 최우선 기준 문서(02)가 자기모순을 일으켜 "충돌 시 02가 이긴다"는 조정 장치가 작동하지 않는 지점 존재 |

세 가설이 모두 High/Strong으로 확증되었다 — 이례적이다. 통상 트레이스는 1개가 이기고 2개가 죽는데, 여기서는 **세 개가 전부 참이고 1번이 나머지의 상류에 있다.**

---

## Convergence / Separation Notes ★ 이 트레이스의 핵심

### 수렴 1 — 세 레인이 같은 지점에 도달 (임베딩 스케일)

| 레인 | 도달 경로 | 결론 |
| --- | --- | --- |
| L1 | 문서 대조만으로 답할 수 없는 유일한 질문으로 **역산** | critical unknown: "S=cos×100에서 0.80/0.92 도달 가능한가?" |
| L2 | OpenAI 커뮤니티·벤치마크 **실측 조회** | [T2] 평균 코사인 43%, `green_threshold=80` 도달 불가 |
| L3 | ada-002(평균 0.85) → 3-small(평균 0.43) **세대 이전 추적** | [A-1] 임계값 세트 전체가 구세대 캘리브레이션의 화석 |

**서로 다른 방법론(정합성 역산 / 실측 조회 / 계보 추적)이 같은 결론에 수렴 — 이 발견의 신뢰도가 가장 높다.**

### 수렴 2 — G가 죽은 코드 (형식 증명)

L1 [B3] · L2 [T4] · L3 [A-2]가 동일 결함을 독립 발견. L3이 형식 증명까지 완성:

```
06-ai-pipeline.md §2⑤ 정의에 따라:
  G_raw < 60 → 무근거 문장 제거 → 남은 문장 전부 supported → G_final = 100
  G_raw ≥ 60 → G_final = G_raw ∈ [60,100]
∴ G_final ≥ 60  (항상)
∴ S ≤ 60 이면 min(S, G_final) = S   ← G가 결과에 개입 불가

그리고 수렴 1에 의해 실측 S ≈ 40~55 < 60
∴ G는 데모 전 구간에서 매칭률에 1점도 기여하지 않는다
```

부작용으로 **등급 함수가 비단조**가 된다: 환각 60%(S90/G40) → 프루닝 발동 → 매칭률 90 🟢 / 환각 39%(S90/G61) → 프루닝 미발동 → 매칭률 61 🟡. **더 나쁜 답변이 더 좋은 등급을 받는다.**

### 수렴 3 — 실패가 지표에 의해 은폐됨

자동응답률은 🟢+🟡 **합계**로 정의(`04 §5`). 따라서 수렴 1이 터져 🟢이 전부 🟡로 무너져도 **이 지표는 0.70을 유지한다.** 데모 마지막 지표 화면은 초록불인데 실제 경험은 "전부 확인 대기, 담당자 큐 10건". → 예상 질문 *"어차피 푸시 올 거면 왜 필요하냐"* 를 **데모가 스스로 실증**한다.

### 분리 — 독립적으로 남는 축

수렴 1이 해결되어도 남는 것: **상태 모델 폐쇄성 결함**(L1 고유). 🟢 확정 경로 부재, `held→answered` 전이 부재, 만료가 카드를 죽임. 이 셋은 임베딩과 무관한 직교 축이며 각각 마이그레이션을 유발한다.

---

## Evidence Summary by Hypothesis

### H1 (L3 전제 결함) — 지지 증거
1. **[외부 1차]** `text-embedding-3-small` 임의 텍스트 쌍 평균 코사인 **0.43**, 실무 권장 유사 임계 0.45 (s-anand). ada-002는 평균 0.85.
2. **[외부 1차]** OpenAI 개발자 포럼: *"같은 쿼리·같은 문서인데 ada는 0.70+, 3-small은 0.4 근처"* → **관련 문서조차 S≈40**.
3. **[형식 증명]** 수렴 2의 G 무력화 증명.
4. **[산술]** 시드 코퍼스 = **6청크 / 271토큰**. `retrieval_top_k=8 > 6` → 매 질의가 전체 코퍼스 반환 → **검색 단계가 no-op**, "근거 0건 → 강제 🔴" 경로 **발동 불가**.
5. **[시간대 계산]** DND 22:00–07:00 ET ≡ **11:00–20:00 KST**. 한국 낮 시연은 100% DND 내부 → D2 규칙에 의해 **Q7·Q8의 🔴이 🟡로 자동 강등** → 데모 2단계 대사("🔴 보류: 근거 부족")와 화면 불일치.
6. **[grep]** `rerank`/`hybrid`/`BM25`/`정규화`/`calibration` **전 문서 0건**. 유일한 관련 언급이 `01 §범위제외`의 **"매칭률 임계값 자동 보정 = 범위 제외"** — 필요한 처방을 스스로 배제.
7. **[테스트 설계]** FakeLLM은 해시 기반 결정적 벡터 → 유사도 무의미 → `08 §3` 기대 등급 재현하려면 하드코딩 필요 → **`tests/test_demo_scenarios.py`는 동어반복이며 캘리브레이션 결함을 검출할 수 없다.**
8. **[발견 시점]** 유일한 검출 수단 `scripts/eval_questions.py`가 **M9(7일차)**에 있고 DoD 게이트가 아님 → **결함이 발표 하루 전에 발견된다.**

### H2 (L2 실현가능성) — 지지 증거 (전부 1차 소스 확인)
1. **pgvector `<=>`는 코사인 거리(0~2)**. `1 - (e <=> q)`가 유사도. SQLAlchemy 헬퍼명도 `cosine_distance()`. **문서 전체 grep에서 거리/변환 언급 0건** → 구현자가 거리를 그대로 `similarity`에 담으면 정렬은 우연히 맞고 **점수는 완전 반전**(가장 잘 맞는 청크가 최저점). FakeLLM 테스트로는 절대 안 잡힌다.
2. **GPT-5 계열은 `temperature` 미지원**(400), `max_tokens` → `max_completion_tokens`. `temperature=0`은 결정적 출력의 표준 관행이라 **반드시 밟는 지뢰**.
3. **Azure 실측 gpt-5-mini 지연 45~120s** 보고. 문서의 "타임아웃 15초·재시도 1회" = 최악 30초 > **전체 예산 20초**. 정상 호출을 죽인다.
4. **FastAPI 0.106.0부터 `yield` 의존성 exit이 BackgroundTasks보다 먼저 실행**(공식 문서 명시). 라우터에서 `db`를 태스크에 넘기는 가장 자연스러운 작성법이 첫 쿼리에서 죽고, **실패 기록 안전망도 같은 죽은 세션을 쓰므로 함께 실패 → 질문이 `processing`에 영구 정지.**
5. **부분 UNIQUE는 DEFERRABLE 불가**(deferrable은 제약 속성, `ALTER TABLE ADD UNIQUE`는 `WHERE` 미지원). 활성 버전 스왑을 단일 UPDATE로 하면 23505. 전환 절차가 문서에 없다.
6. **strict Structured Outputs는 전 필드 required + `additionalProperties:false` 요구.** Pydantic v2 기본 스키마 직행은 400 — `conflict_chunk_ids: []`가 정확히 그 케이스. **M3 첫 실행에서 100% 재현.**
7. **HNSW는 post-filtering** → 프로젝트/활성버전 필터가 선택적이면 `LIMIT k` 미달. **"결과 0건 → 강제 🔴"이 근거가 있는데도 발동**(조용한 오탐). 데모(프로젝트 1개)에서는 절대 재현 안 되고 심사위원이 새 프로젝트를 만드는 순간 터진다.
8. **다중 워커 = SSE 붕괴 + 스케줄러 N중 실행.** 문서에 "단일 프로세스" 명시 없음, `--workers 1` 없음. "같은 날 중복 발송 금지"는 TOCTOU로 불충분(원자적 `ON CONFLICT DO NOTHING` 필요).
9. **일정: 엔드포인트 49개·테이블 16개 → 96~153h.** 7일 환산 하루 14~22시간. Claude Code 2배 가정해도 하루 9~13시간 연속.

### H3 (L1 정합성) — 지지 증거
1. **최우선 기준 문서의 자기모순.** `02 §1` *"🟢 … 담당자에게 알리지 않는다"* ↔ `02 §3` *"맞았다 2건 이상 쌓인 답변은 담당자 브리핑 목록 맨 위에 승인 추천으로 올린다"*. 🟢에는 카드가 없으므로 후자가 성립 불가. **문서 우선순위 규칙(02 > 05 > 06)이 02 내부 충돌에는 적용되지 않는다** — 조정 장치 자체에 구멍.
2. **결과: 🟢은 어떤 경로로도 `verified`가 될 수 없고 100% 72h 만료.** 확정 전이를 일으키는 API가 카드 액션뿐이므로. **지식 환류 루프가 트래픽 다수(목표 70%)에 대해 완전히 끊긴다.**
3. **`held → answered` 전이가 문서 전체에 없다.** `05 §6`은 `held`면 `answer: null`을 강제 → 담당자가 🔴 카드를 확정해도 질문자 화면은 여전히 null → **데모 4단계("답변이 확정으로 갱신") 불성립.** M4 DoD는 질문자 관점 GET을 검증하지 않아 **M9 리허설에서 처음 발견**된다.
4. **계약 필드에 저장소가 없는 사례 3건**: `held_info.reason`(컬럼 없음), `bulk-keep {document_version_id}`(review_cards에 없음), `pending_briefing`(notifications에 없음). **API 계약서와 데이터 모델이 서로를 검토한 흔적이 없다.**
5. **같은 섹션 안에서 존재하지 않는 식별자 참조**: `04 §5`가 22종 이벤트를 열거한 직후 지표 표에서 `answer.reused + reuse 실패 이벤트`를 참조 — 목록에 없다.
6. **사양이 자기 테스트와 충돌**: `06 §2⑤`의 `min(S,100)=S` vs `06 §5 테스트1`의 "min 규칙으로 강등".
7. **만료 스위퍼가 살아 있는 카드 밑에서 답변을 죽인다** — 담당자 `defer`(금) → 72h(월) → `expired` → 카드 approve 시 `expired→verified` 전이가 다이어그램에 없음. **"인박스가 최종 안전망"이 깨진다.**
8. **`citations[]`에 `document_id`/`document_version_id` 부재** → 근거 원문 열람 URL 구성 불가 → 기능 2.2가 계약서상 구현 불가. 데모 1단계의 "원문 보기"가 클릭 한 번에 끊긴다.

---

## Evidence Against / Missing Evidence

### 문서가 이미 올바르게 처리한 것 (세 레인 공통 확인)
1. **모델 ID가 전부 실재한다** — `gpt-5-mini`(스냅샷 `gpt-5-mini-2025-08-07`, deprecation 없음, Structured Outputs 지원), `text-embedding-3-small` **1536차원 정확**. 존재하지 않는 모델을 지어내지 않았다.
2. **`vector_cosine_ops` + HNSW 선택이 정확**하고 1536은 인덱싱 상한(2000) 안. HNSW는 데이터 없이도 생성 가능해 M0/M2 배치와 일치.
3. **부분 UNIQUE 제약 자체는 합법** — 문제는 제약이 아니라 전환 절차 누락.
4. **`?token=` SSE 인증 판단이 옳다** — EventSource 헤더 제약은 실재하고 쿼리 토큰이 표준 우회책. "SSE 한정" 스코프 축소도 정확. 15초 ping도 Railway 요건(5분) 충족.
5. **테스트에 Postgres 강제가 옳다** — pgvector 때문에 SQLite 실제 불가. `aiosqlite 대신`이라고 명시 배제한 것은 함정을 밟아본 판단.
6. **역추적 조인 경로가 실재한다** — `answer_citations → chunks → document_versions → documents` 연결됨. 재검토 연쇄는 스키마상 가능(결함은 그룹핑 키 부재와 재사용 답변 누락에 한정).
7. **알려진 룰 충돌 3건을 선제 봉합**(D2/D9/§10 구현 노트) — 상충 지점을 **명시적으로 호명하고** 결정을 남긴 수준은 흔치 않다.
8. **TODO 인계 4/4 폐쇄** — M1→M4, M2→M4, M3→M4, M3→M6 전부 열리고 전부 닫힘. 의심했던 인계 누락은 발생하지 않았다.
9. **임계값 설정화 8/8** — CLAUDE.md가 나열한 값이 `04 §3` settings JSON에 전부 존재. 원칙만 선언하고 저장소를 안 만드는 흔한 실패를 피함.
10. **인박스 안전망 원칙이 3개 문서에서 일관 반복**되고 프롬프트의 테스트 요구까지 내려와 있음.
11. **`run_in_executor`로 동기 파싱 분리** — pypdf/python-docx의 이벤트 루프 블로킹을 정확히 인지.
12. **컷라인 문서 존재 + "절대 컷 불가" 명시** — 1주 계획으로는 드물게 성숙. 순서만 조정하면 됨.
13. **비용은 문제가 아니다** — 질문당 ~$0.005(≈7원), 일일 상한 500콜 = $0.6/일. 해커톤 전 기간이 커피 한 잔 미만. "비용 예산 비현실적" 의심은 **기각**.
14. **담당자 1명/프로젝트 1개 전제는 데모와 완전 정합**하고 부분 UNIQUE로 강제까지 되어 있음. **깨지지 않는다.**

### 결정적 누락 증거
- **실측 데이터 0건.** 이 조사 전체에서 시드 코퍼스에 대한 실제 임베딩 유사도가 한 번도 측정되지 않았다. 모든 Blocker 판정이 외부 실증에 기반한 **추론**이다.
- `gpt-5-mini`의 `reasoning_effort` 지원 여부 미확인(모델 페이지가 지원 파라미터를 열거하지 않음).
- `pgvector/pgvector:pg16` 이미지의 번들 pgvector 버전 미확인(0.8.0 이상이어야 `hnsw.iterative_scan` 사용 가능).
- Railway/Render 매니지드 Postgres가 `CREATE EXTENSION vector` 권한을 주는지 미확인 — 안 주면 M9가 통째로 막힘.

---

## Per-Lane Critical Unknowns

- **Lane 1 (내부 정합성)**: `02 §1`의 `S = 코사인 유사도 × 100` 점수화에서, `text-embedding-3-small`과 `08 §2` 시드 코퍼스로 `green_threshold=80`·`reuse_threshold=0.92`에 **실제로 도달 가능한가?** — 문서만으로는 원리적으로 답할 수 없는 유일한 질문이며, 답이 "아니오"면 최우선 기준 문서의 핵심 수치가 바뀐다.
- **Lane 2 (실현가능성)**: `gpt-5-mini`의 `reasoning_effort` 지원 여부와 `minimal`에서의 실제 종단 지연 — 20초 예산의 생사. 부수적으로 pgvector 이미지 버전, Railway extension 권한.
- **Lane 3 (전제)**: 해커톤 **시연 시각(KST)** 과 **프론트 팀 D-day** — 전자는 DND 강등 발화 여부를, 후자는 "백엔드 온리인데 6개 화면이 필요한" 리스크의 심각도를 확정한다. 둘 다 문서 밖 정보.

---

## Rebuttal Round

**리더(H1 전제 결함)에 대한 최강 반박 — H3(정합성) 진영:**
> "L1이 찾은 결함 30건 중 임베딩과 무관한 것이 27건이다. 특히 [B1] 🟢 확정 경로 부재는 임베딩이 완벽해도 남으며, 오히려 임베딩이 잘 작동해 🟢이 많아질수록 **더 심각해진다**(확정되지 못하고 만료되는 답변이 늘어남). 지식 환류 루프 — 이 서비스의 4대 컨셉 중 하나 — 가 통째로 끊긴다. 근본 원인은 임베딩이 아니라 상태 모델이다."

**리더가 버틴 이유:**
반박은 부분적으로 타당하며 [B1]은 실제로 독립 Blocker다. 그러나 세 가지가 리더를 지킨다.
1. **해소 비용의 비대칭.** L1의 27건은 전부 문서 수정으로 며칠 내 해소 가능하고 **지금 당장 확정적으로 고칠 수 있다**. H1은 실측 없이는 수정 방향조차 정할 수 없다(임계값을 얼마로? 리스케일 파라미터는?).
2. **발견 시점의 비대칭.** L1 결함은 구현 중 컴파일/테스트가 잡아주거나 최소한 M4 e2e에서 드러난다. H1은 **FakeLLM 테스트를 전부 통과한 채로** M9 클라우드 데모에서 처음 드러난다 — 복구 시간 0.
3. **파급 범위.** H1이 참이면 `08 §3` 기대 등급표 10건 중 7건 무효, `06 §5` 테스트 4 재작성, M3·M4·M9 DoD 동시 실패, `02 §1` 등급 정의와 `04 §3` 기본값 전면 재산정. **최우선 기준 문서의 핵심 수치가 바뀐다.**

**판정: 병합이 아니라 직교.** H1과 H3은 서로 다른 축이며 **둘 다 처리해야 한다.** H1은 "측정 후 결정", H3은 "지금 확정 수정". 순서는 H1 측정을 먼저 걸어두고 그동안 H3을 고치는 것이 최적.

---

## Most Likely Explanation

**문서 세트는 내부 서술 품질과 자기 인식(우선순위 규칙·결정 노트·컷라인·안전망 원칙)이 평균을 크게 상회하지만, 단 하나의 외부 사실 — `text-embedding-3-small`의 실제 유사도 스케일 — 을 검증하지 않은 채 그 위에 핵심 차별점(매칭률 3분기)과 데모 클라이맥스(공식 Q&A 재사용) 전체를 올려놓았다.**

임계값 세트(80/50/0.92/0.85)는 평균 코사인 0.85이던 `ada-002` 시대의 감각으로 캘리브레이션된 값이며, 평균 0.43인 `text-embedding-3-small`에 그대로 이식되었다. 그 결과 🟢은 수학적으로 도달 불가, 🔴 경계는 노이즈 평균보다 7점 위, 재사용은 거의 동일 문장에서만 발동한다. 여기에 (a) G 프루닝이 `min(S,G)`의 G를 죽은 코드로 만들고, (b) 자동응답률이 🟢+🟡 합계라 이 실패를 완벽히 은폐하며, (c) 검증 수단(FakeLLM 테스트)이 해시 기반 벡터를 쓰므로 동어반복이 되어 **결함을 검출할 수 없다**.

이와 **독립적으로**, 상태 모델에 3개의 폐쇄성 결함(🟢 확정 경로 부재 / `held→answered` 전이 부재 / 만료가 살아 있는 카드 밑에서 답변을 죽임)과, 계약 필드에 저장소가 없는 사례 3건이 있어 구현 중 데이터 모델 마이그레이션과 API 재배포(프론트 재작업)를 유발한다.

가장 개연성 높은 데모 결과: **모든 질문이 🟡로 수렴 → 담당자 큐에 10건 전부 적재 → 예상 질문 "어차피 푸시 올 거면 왜 필요하냐"를 데모가 스스로 실증**, 그리고 마지막 지표 화면의 `requestion_instant_rate: 0.0`(목표 0.95)이 자기 반박으로 표시된다.

---

## Critical Unknown

> **시드 코퍼스 6청크에 대한 Q1~Q10의 실제 top-1 코사인 값, 그리고 Q8↔Q10 질문 임베딩 유사도.**

이 두 숫자가 **A-1·A-2·B-1·B-3·T2·T3의 심각도를 전부 동시에 결정**한다. 현재 모든 Blocker 판정은 공개 실증(0.40~0.55)에 기반한 추론이며, 이 코퍼스·이 질문에 대한 측정이 0건이다. API 명세 같은 짧고 사실적인 텍스트는 일반 산문보다 유사도가 높게 나올 수 있어 0.65~0.75 가능성을 배제할 수 없다(그래도 0.80은 어렵다고 본다).

부차적으로: `reasoning_effort` 지원 여부(20초 예산의 생사), 시연 시각 KST(DND 발화 여부), 프론트 D-day.

---

## Recommended Discriminating Probe

**단일 스크립트로 Blocker 6건(T2·T3·T5·A-1·A-2·B-3)의 실재 여부와 수정 방향을 동시에 확정한다. 소요 10분, 비용 <$0.01, OpenAI 키 1개, 백엔드 코드 불필요 — 지금 즉시 실행 가능.**

`scripts/probe_calibration.py`:
1. `08 §2` 시드 3문서를 헤딩 단위로 손분할(6청크) → 임베딩
2. Q1~Q10의 영어 번역문 → 임베딩
3. **판정 1 (T2/A-1)**: 각 질문의 top-1 코사인과 `S = round(cos×100)`, `등급@80/50` 출력
4. **판정 2 (T3/B-3)**: Q8↔Q10 질문 임베딩 유사도 출력 (0.92 도달 여부)
5. **판정 3 (T5)**: `reasoning_effort=minimal|medium`으로 strict JSON 스키마 호출 1회씩 → 지연 측정 + 400 여부(파라미터 지원 확인)
6. **판정 4 (B-1)**: Q9(무근거)의 top-1이 Q1~Q6 대역과 겹치는지 → 유사도만으로 🔴 분리 가능한지

**사전 등록 판정 기준**

| 관측 | 결론 | 액션 |
| --- | --- | --- |
| Q1~Q6 중 S≥80이 0개 | **T2/A-1 확정 Blocker** | `s_floor`/`s_ceil` 리스케일 도입 (권장) 또는 임계값 재산출 |
| Q1~Q6 S가 대부분 <50 | 최악 — 데모 전부 🔴 | 리스케일 + `top_k=4` + 시드 확장 전부 필수 |
| Q1~Q6 중 S≥80이 4개 이상 | **A-1 기각** | 임계값 유지, 나머지 축만 처리 |
| Q8↔Q10 < 0.92 | **T3/B-3 확정 Blocker** | `reuse_threshold` 재설정 + LLM 동일성 게이트 추가 |
| `reasoning_effort` 400 | 미지원 확정 | 20초 예산 포기 또는 비추론 모델로 교체 |
| minimal 지연 ×4 > 20s | **T5 확정 Blocker** | 성능 목표 재기술 + ①/⑧ 병렬화 또는 ④⑤ 병합 |
| Q9 top-1 ≥ Q1~Q6 최소값 | 유사도로 🔴 분리 불가 | `not_answerable` 프롬프트 강화에 전적 의존 |

**보조 프로브 (5분, DB만 필요 — T1·T8·T10 판정)**
```sql
CREATE EXTENSION IF NOT EXISTS vector;
SELECT extversion FROM pg_extension WHERE extname='vector';   -- T10: 0.8.0 이상?
SELECT '[1,0,0]'::vector <=> '[1,0,0]'::vector AS same,        -- T1: 기대 0 (거리!)
       '[1,0,0]'::vector <=> '[0,1,0]'::vector AS orthogonal;  --     기대 1
CREATE TABLE t(id int primary key, doc int, act bool);
CREATE UNIQUE INDEX ON t(doc) WHERE act;
INSERT INTO t VALUES (1,1,true),(2,1,false);
UPDATE t SET act = (id = 2) WHERE doc = 1;                     -- T8: 23505 나면 확정
```

**왜 이 probe가 최적인가**: 실행 15분에 Blocker 10건 중 8건의 실재 여부가 확정된다. 그리고 **판정 1의 출력 표가 그대로 IR Deck 슬라이드 1장**("우리는 임계값을 실측으로 캘리브레이션했다")이 되어, 예상 질문 *"매칭률 자기평가는 순환논리 아닌가?"* 에 대한 최강 답변 자료가 된다. **결함 조사 산출물이 그대로 발표 자산이 되는 드문 경우.**

실행 시점은 **M0 착수 전** — `07-build-plan.md`의 M9(7일차) `eval_questions.py`까지 기다리면 잘못된 임계값 위에 8종 테스트를 작성하는 낭비가 발생한다.

---

## 부록 A — 통합 결함 등록부 (Blocker)

| # | 결함 | 출처 | 영향 |
| --- | --- | --- | --- |
| **BL-1** | 임계값 80/50/0.92/0.85가 ada-002 캘리브레이션 → 3-small에서 🟢 도달 불가 | L1-unknown + L2-T2 + L3-A1 ★수렴 | 데모 전 구간 🟡 수렴, 차별점 소멸 |
| **BL-2** | pgvector `<=>`는 코사인 **거리** — 문서 전체가 "유사도"로만 서술(변환 언급 0건) | L2-T1 | 점수 완전 반전, FakeLLM으로 검출 불가 |
| **BL-3** | 재사용 0.92 미도달 → Q10 클라이맥스 실패 + 지표 자기반박 | L2-T3 + L3-B3 | 데모 5·6단계 붕괴 |
| **BL-4** | G 프루닝이 min(S,G) 무력화 + 등급 함수 비단조 | L1-B3 + L2-T4 + L3-A2 ★수렴 | 환각 심할수록 높은 등급 |
| **BL-5** | 🟢에 카드가 없어 확정 경로 부재 → 100% 만료, 지식 환류 끊김 | L1-B1 (02 문서 자기모순) | 4대 컨셉 중 하나 소멸 |
| **BL-6** | `held → answered` 전이 부재 + 계약이 held면 answer=null 강제 | L1-B2 | 데모 4단계 불성립, M9에서 첫 발견 |
| **BL-7** | gpt-5-mini `temperature` 400 + 15초 타임아웃이 정상 호출 살해 + 20초 예산 붕괴 | L2-T5 + L3-E3 | 파이프라인 전체 failed |
| **BL-8** | DND 22:00–07:00 ET ≡ 11:00–20:00 KST = 데모 시간대 전체 → 🔴이 🟡로 강등 | L3-B4 | "정직하게 보류" 시연 불가 |
| **BL-9** | 시드 6청크 < top_k 8 → 검색 no-op, `no_evidence` 경로 발동 불가 | L3-B1 | 등급이 단일 코사인으로 환원 |
| **BL-10** | `GET /review-cards` 목록 아이템 shape가 계약서에 없음(상세만 존재) | L3-C3-1 | 프론트가 큐 화면을 그릴 수 없음 |

## 부록 B — High 결함 요약

**기술(L2)**: T6 BackgroundTasks 세션 수명(질문이 `processing` 영구 정지) · T7 다중 워커 SSE/스케줄러(`--workers 1` 명시 없음) · T8 부분 UNIQUE 스왑 절차 부재(23505) · T9 strict 스키마 400(M3 첫 실행 100% 재현) · T10 HNSW post-filtering 오탐

**정합성(L1)**: H1 재사용 답변 상태/만료/이벤트 미정의 · H2 `held_reason` 저장소 없음 · H3 review_cards에 문서 버전 링크 없음(bulk-keep 구현 불가) · H4 공식 Q&A 근거 경로 4중 미완성 · H5 만료가 살아 있는 카드를 죽임 · H6 타임존 단일 원천 없음(등급에 영향) · H7 지표가 없는 이벤트 참조 · H8 미검증 임베딩 가정

**전제/계약(L3)**: C-0 citation에 `document_id` 부재(근거 열람 불가) · C1-8 i18n 소유 주체 미정의 · C2-2 선택지 탭 응답 API 부재("30초 컷" 불성립) · C2-3 프리페치가 지표 파괴 · C0-1 인제스트 완료 SSE 없음 · C0-2 SSE 토큰 URL 노출 · D-3 프론트 팀 일정 문서 0건(6개 화면 소유자 없음) · D-1 "4겹 방어" 정의 0건 · D-2 학습 곡선 시계열 API 없음

**일정(L2)**: 96~153h ÷ 7일 = 하루 14~22h. 병목은 M3이 아니라 **6일차 M5+M6(20~30h)**. 배포를 6일차로 앞당길 것 권고.
